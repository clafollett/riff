node_modules/
dist/
*.log
.env
.env.*
!.env.example

# The village's own state. This is a SEPARATE git repo — `git log` in there
# is the record of what the staff did while you were away. Not our history.
/world/

# Derived, rebuildable from world/ + event log.
/var/
