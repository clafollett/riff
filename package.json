{
  "name": "bed-and-breakfast",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A village where AI staff live, work, and run real parts of your life — under rules enforced in code, not prompts.",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "check": "tsgo --noEmit",
    "test": "node --test test/**/*.test.ts",
    "inn": "node src/gateway/server.ts",
    "seed": "node scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@typescript/native-preview": "latest",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.243",
    "zod": "^4.4.3"
  }
}
